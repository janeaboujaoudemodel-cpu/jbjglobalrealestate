# Fix: Remove broken baseline PDF + regenerate clean premium baseline

## What you saw

The file you opened ("33 minutes ago" in Recent, but the actual artifact dated 5 days ago) is **`/mnt/documents/baselines/company-profile_baseline.pdf`** — a 26 MB **diff** PDF generated by the old `pdf_export_gate.py` run on Apr 22. It is not the company profile itself; it is the comparison overlay where the pink/red regions mark "shifted/leaked" content (sidebar, filter bar, cookie banner, FAB stack) and the rest of the pages render blank because that diff job only painted areas that mismatched.

The real, served document — `public/documents/JBJ-Global-Real-Estate-Company-Profile.pdf` (5.2 MB, 18 pages) — is intact. The clean v2 baseline I rendered yesterday (`company-profile_baseline_v2.pdf`, 2.1 MB) is also fine. Only the legacy diff artifacts need to go.

## Plan

### 1. Delete the broken legacy baseline artifacts
Remove from `/mnt/documents/`:
- `baselines/company-profile_baseline.pdf` (26 MB, Apr 22 — pink/blank diff)
- `baselines/company-profile_baseline.json` (companion metadata)
- `pdf_baselines/company-profile/baseline.pdf` (26 MB duplicate)
- `pdf_baselines/company-profile/pages/` (per-page diff PNGs)
- `pdf_baselines/company-profile/reports/` (old diff reports)
Keep `profile.json` + `tolerances.json` (they describe the page model, not the broken render).

### 2. Regenerate a single, premium, clean baseline
- Boot the preview, navigate to `/company-profile?print=1` (the print-mode wrapper is now fully chrome-free per the v2 fix).
- Render to PDF with Playwright at A4, full-page, every section expanded.
- Save as **`/mnt/documents/company-profile_baseline.pdf`** (overwriting any stale name) — this becomes the single, canonical baseline.
- Also drop `company-profile_baseline_v3.pdf` next to it as the dated copy so version history stays intact.

### 3. Visual QA every page (mandatory)
- Rasterize all pages of the new baseline at 150 DPI.
- Inspect each page image: confirm zero pink/red overlay, no blank pages, no chrome leakage (sidebar, filter bar, cookie banner, FABs), no clipped text, hero section fully painted.
- If any page fails, fix the underlying CSS/component leak and re-render before delivering.
- Save the QA strip as `company-profile_baseline_qa_strip.pdf`.

### 4. Cross-check the served PDF
- Rasterize the 18 pages of `public/documents/JBJ-Global-Real-Estate-Company-Profile.pdf` and confirm it still renders cleanly end-to-end (this is what the public Download button serves).
- Confirm `CompanyProfileDownload` admin card's three buttons (Download, Open Clean PDF, Open Page Baseline) all resolve.

### 5. Deliverables in `/mnt/documents/`
- `company-profile_baseline.pdf` — fresh, clean, canonical baseline (replaces the broken 26 MB one).
- `company-profile_baseline_v3.pdf` — same render, dated copy.
- `company-profile_baseline_qa_strip.pdf` — full-page QA contact sheet.
- Removed: every Apr 22 diff artifact listed in step 1.

## Technical notes

- No code changes required — the print-mode chrome leakage was already fixed in v2 (`data-chrome` markers + `index.css` print rules). This pass is purely artifact cleanup + a fresh render.
- The served public PDF asset is untouched.
- No DB, no edge function, no migration changes.
