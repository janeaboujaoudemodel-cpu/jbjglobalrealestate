I’ll rebuild the AI Hub page around one premium neon tools experience:

1. Hero rebuild
- Keep the existing AI Hub video background, but make it visibly stronger and readable with a darker cinematic overlay, neon purple/cyan accents, and high-contrast white title/subtitle.
- Restyle the three hero CTAs as premium dark/neon buttons with white text/icons and better spacing.
- Add category filter chips directly in the page header/hero area so users can jump/filter immediately.

2. Tools section simplification
- Keep one main “Discover All Free AI Tools” grid only.
- Remove the repeated category sections like “Investment & Property Tools” and “Design & Media Tools.”
- Remove the shortcut/suite row that currently makes the section feel cluttered.
- Wire filter chips to the same tool grid so selecting Property, Corporate, Productivity, Design/Media, or Marketing shows only matching tools.
- Keep search readable with dark/neon styling.

3. Portal CTA logic by user mode
- Remove the separate Broker Portal / Investor Hub row completely.
- Replace it with one mode-aware CTA in the hero or primary tools header:
  - broker mode: Broker Portal
  - investor mode: Investor Portal
  - developer mode: Developer Portal
- Use the existing user mode context and correct portal routes.

4. Page visual system
- Move the AI Hub away from beige/white cards into a smarter neon-dark AI aesthetic while preserving JBJ brand rules.
- Tool cards become dark glass/neon cards with clear icons, readable text, and consistent padding.
- The benefits strip becomes part of the neon command-center style instead of champagne cards.

5. Bottom section fix
- Restyle the “Welcome back / Start using tools” bottom section so it is clearly different from the footer color.
- Avoid any bottom page band matching the footer.

6. Validation
- After implementation, open `/ai-hub`, capture the page, check hero readability, CTA contrast, filter behavior, and confirm the removed sections no longer appear.