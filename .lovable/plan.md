## Immediate priority: repair Omar signed-document failure\n\n1. **Fix signed PDF generation**\n   - Correct existing template field coordinates that are still stored as old pixel values (e.g. `y_position` over 100) so signatures/dates flatten inside the visible signature block.\n   - Update the signing completion backend to normalize legacy coordinates before drawing fields into the final PDF.\n   - Ensure date fields are filled only at signing time and written into the same date area shown in the signing preview.\n\n2. **Fix the “signed but