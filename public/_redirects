# Static assets - never rewrite to index.html
/assets/*  /assets/:splat  200
/sw.js  /sw.js  200
/sw-kill.js  /sw-kill.js  200
/favicon.png  /favicon.png  200
/apple-touch-icon.png  /apple-touch-icon.png  200
/og-image.jpg  /og-image.jpg  200
/manifest.webmanifest  /manifest.webmanifest  200

# Branded file proxy — keeps document email downloads on jbj.ae instead of
# exposing the raw backend storage/function host that desktop blockers target.
/api/download-file  https://mdafrewypkkrildjgtey.supabase.co/functions/v1/download-file  200

# SPA fallback - must be LAST
/*  /index.html  200
