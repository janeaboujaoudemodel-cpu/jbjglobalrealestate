project_id = "mdafrewypkkrildjgtey"

# Public-facing functions: they authenticate callers themselves (guests are
# allowed on the public site, gated-portal calls are rejected with 401
# auth_required inside the function), so platform JWT verification is off.
[functions.ai-chat-support]
verify_jwt = false

[functions.ai-chat-stream]
verify_jwt = false

[functions.chat-support-notify]
verify_jwt = false

[functions.advisory-desk-request]
verify_jwt = false

[functions.company-profile-notify]
verify_jwt = false
